---
title: 'Ubuntu 설정 마지막'
---

---

### 스왑 메모리 확인

```bash
free -m
# 단위표시
free -h
```

### 에러

```
push access denied, repository does not exist or may require authorization: server message: insufficient_scope: authorization failed
```

Docker Hub에 push중에 만난 에러

도커 로그인을 `sudo`를 붙여서 `sudo docker login -u [name]` 으로 하니까 작동한다
