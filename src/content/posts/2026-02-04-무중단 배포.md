---
title: '무중단 배포'
published: 2026-02-04
description: 'pc 한 대로 무중단 배포?'
pinned: false
author: 'necteo'
image: ''
tags: ['Ubuntu', 'Docker', 'Jenkins', 'Kubernetes', 'nginx']
category: 'CI/CD'
draft: false
---

## Minikube에 무중단 배포

**build.gradle**

```gradle
dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-actuator'
}
```

**application.yml**

```yml
server:
  port: 9090
  shutdown: graceful

spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
```

**deployment.yaml**

```yml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: totalapp-deployment
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: totalapp
  template:
    metadata:
      labels:
        app: totalapp
    spec:
      terminationGracePeriodSeconds: 35
      containers:
        - name: totalapp
          image: necteo/total-app
          imagePullPolicy: Always
          ports:
            - containerPort: 9090
          startupProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9090
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 20 # 최대 100초까지 대기
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9090
            periodSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9090
            periodSeconds: 10
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ['sh', '-c', 'sleep 5']
---
apiVersion: v1
kind: Service
metadata:
  name: totalapp-service
spec:
  selector:
    app: totalapp
  ports:
    - port: 80
      targetPort: 9090
  type: NodePort
```

**Jenkinsfile**

```groovy
pipeline {
  agent any

  environment {
    LANG = 'ko_KR.UTF-8'
    LC_ALL = 'ko_KR.UTF-8'
  }

  stages {
    stage('Check Out') {
      steps {
        echo 'Git Checkout'
        checkout scm
      }
    }

    // gradlew build => war파일을 다시 생성
    stage('Gradle Permission') {
      steps {
        sh 'chmod +x gradlew'
      }
    }

    // build 시작
    stage('Gradle Build') {
      steps {
        sh './gradlew clean build'
      }
    }

    stage('Docker Build') {
      steps {
        sh '''
          docker build -t necteo/total-app:${BUILD_NUMBER} .
        '''
      }
    }

    stage('Docker Push') {
      steps {
        // 젠킨스에 등록한 자격 증명을 사용하여 로그인 및 푸시
        withCredentials([usernamePassword(credentialsId: 'docker-hub-id',
                         passwordVariable: 'DOCKER_PASSWORD',
                         usernameVariable: 'DOCKER_USERNAME')]) {
          sh "docker login -u ${DOCKER_USERNAME} -p ${DOCKER_PASSWORD}"
          sh "docker push necteo/total-app:${BUILD_NUMBER}"
        }
      }
    }

    // 실행 명령
    stage('Deploy to MiniKube') {
      steps {
        sh '''
          kubectl set image deployment/totalapp-deployment totalapp=necteo/total-app:${BUILD_NUMBER}
          kubectl rollout status deployment/totalapp-deployment --timeout=120s
        '''
      }
    }
  }

  post {
    success {
      echo '실행 성공'
    }
    failure {
      sh 'kubectl rollout undo deployment/totalapp-deployment || true'
      echo '실행 실패'
    }
  }
}
```

### 문제점

```bash
:~$ kubectl logs -l app=totalapp --tail=100 | grep "Started"
2026-02-04T07:50:19.027Z  INFO 1 --- [           main] c.s.web.SpringTotalProjectApplication    : Started SpringTotalProjectApplication in 18.582 seconds (process running for 29.455)
2026-02-04T07:50:16.615Z  INFO 1 --- [           main] c.s.web.SpringTotalProjectApplication    : Started SpringTotalProjectApplication in 30.275 seconds (process running for 35.891)
```

어플리케이션 시작 시간이 무려 30초까지 걸린다

**결국 PC 1대 + 앱 시작 30초 = 무중단 불가**

> `replicas: 2`여도 같은 PC에서 리소스를 공유하기 때문에
>
> 1. 새 Pod 시작 → CPU/메모리 점유
> 2. 기존 Pod 성능 저하
> 3. 30초 동안 새 Pod 준비 중
> 4. 그 사이 트래픽 처리 불안정

따라서 Minikube를 버리고 로컬 도커에서 무중단 배포를 해보는 걸로 변경

## Docker로만 배포

### 작동 방식

nginx로 포트포워딩 => 포트 스위치(Blue-Green) 숨기는 역할

배포마다 9090/9091 포트 스위치

### nginx 설치 및 설정

**1. Nginx 설치**

```bash
bashsudo apt update
sudo apt install nginx -y
```

**2. 설치 확인**

```bash
bashsudo systemctl status nginx
```

active (running) 나오면 성공

**3. 설정 파일 생성**

```bash
bashsudo nano /etc/nginx/conf.d/totalapp.conf
```

```nginx
nginxupstream totalapp {
    server localhost:9090;
}

server {
    listen 80;

    location / {
        proxy_pass http://totalapp;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

**4. 기본 설정 비활성화**

```bash
bashsudo rm /etc/nginx/sites-enabled/default
```

**5. 설정 테스트 및 재시작**

```bash
bashsudo nginx -t
sudo systemctl reload nginx
```

### Jenkins 설정

```groovy
pipeline {
  agent any

  environment {
    LANG = 'ko_KR.UTF-8'
    LC_ALL = 'ko_KR.UTF-8'
  }

  stages {
    stage('Check Out') {
      steps {
        checkout scm
      }
    }

    stage('Gradle Permission') {
      steps {
        sh 'chmod +x gradlew'
      }
    }

    stage('Gradle Build') {
      steps {
        sh './gradlew clean build'
      }
    }

    stage('Docker Build') {
      steps {
        sh "docker build -t necteo/total-app:${BUILD_NUMBER} ."
      }
    }

    stage('Docker Push') {
      steps {
        withCredentials([usernamePassword(credentialsId: 'docker-hub-id',
                         passwordVariable: 'DOCKER_PASSWORD',
                         usernameVariable: 'DOCKER_USERNAME')]) {
          sh "docker login -u ${DOCKER_USERNAME} -p ${DOCKER_PASSWORD}"
          sh "docker push necteo/total-app:${BUILD_NUMBER}"
        }
      }
    }

    stage('Blue-Green Deploy') {
      steps {
        sh '''#!/bin/bash
          # 현재 Nginx가 바라보는 포트 확인
          CURRENT_PORT=$(grep -oP "server localhost:\\K[0-9]+" /etc/nginx/conf.d/totalapp.conf)

          if [ "$CURRENT_PORT" == "9090" ]; then
            NEW_PORT=9091
            OLD_PORT=9090
          else
            NEW_PORT=9090
            OLD_PORT=9091
          fi

          echo "현재: $CURRENT_PORT → 새로운: $NEW_PORT"

          # 새 컨테이너 실행
          docker rm -f app-$NEW_PORT || true
          docker run -d --name app-$NEW_PORT -p $NEW_PORT:9090 necteo/total-app:${BUILD_NUMBER}

          # Ready 대기 (최대 90초)
          echo "앱 시작 대기 중..."
          READY=false
          for i in $(seq 1 45); do
            if curl -s http://localhost:$NEW_PORT/actuator/health/readiness | grep -q "UP"; then
              echo "새 컨테이너 Ready!"
              READY=true
              break
            fi
            sleep 2
          done

          # Ready 안 되면 실패 처리
          if [ "$READY" != "true" ]; then
            echo "컨테이너 시작 실패"
            docker rm -f app-$NEW_PORT || true
            exit 1
          fi

          # Nginx 포트 전환
          sudo sed -i "s/localhost:[0-9]*/localhost:$NEW_PORT/" /etc/nginx/conf.d/totalapp.conf
          sudo nginx -s reload

          echo "트래픽 전환 완료"

          # 잠시 대기 후 기존 컨테이너 종료
          sleep 5
          docker stop app-$OLD_PORT || true
          docker rm app-$OLD_PORT || true

          echo "배포 완료: localhost:$NEW_PORT"
        '''
      }
    }
  }

  post {
    success {
      echo '배포 성공'
    }
    failure {
      echo '배포 실패'
    }
  }
}
```

```bash
sudo visudo

# Cmnd alias specification
# User privilege specification
root    ALL=(ALL:ALL) ALL
sist    ALL=(ALL:ALL) ALL
jenkins ALL=(ALL) NOPASSWD: /usr/sbin/nginx, /bin/sed # <<< 추가
# Members of the admin group may gain root privileges
%admin  ALL=(ALL) ALL
# Allow members of group sudo to execute any command
%sudo   ALL=(ALL:ALL) ALL
```

```bash
# Jenkins 열기
ngrok http 8080
```

**빌드 배포 후**

`localhost(:80)` 접속

**배포 중 확인**

```bash
bashwatch -n 1 'curl -s http://localhost/actuator/health && echo ""'
```

다운타임 없이 계속 `{"status":"UP"}` 나오면 성공
