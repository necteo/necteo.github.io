---
title: 'Jenkins'
published: 2026-01-26
description: 'Jenkins'
pinned: false
author: 'necteo'
image: ''
tags: ['Jenkins']
category: 'CI/CD'
draft: false
---

### Jenkins 설치 및 실행

```bash
sudo apt-get update

sudo wget -O /usr/share/keyrings/jenkins-keyring.asc \
https://pkg.jenkins.io/debian-stable/jenkins.io-2026.key

echo "deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] \
https://pkg.jenkins.io/debian-stable binary/" | \
sudo tee /etc/apt/sources.list.d/jenkins.list

sudo apt-get update

sudo apt-get install jenkins -y

sudo systemctl start jenkins
sudo systemctl enable jenkins

sudo ufw allow 9090

sudo cat /var/lib/jenkins/secrets/initialAdminPassword
411a1a7010664a1f8b60c156160dba01
```

docker 이미지 빌드 후 hub에 올리기 (예전글 참조)

```bash
sudo usermod -aG docker jenkins
sudo systemctl restart jenkins
```

FreeStyle project (연습용)

Build Steps
+Add build step
Execute shell

```bash
docker stop jenkins-app || true
docker rm jenkins-app || true
docker pull necteo/jenkins-app:latest
docker run -d --name jenkins-app -p 9090:9090 necteo/jenkins-app:latest
```

### 가상 아이피

```bash
sudo apt-get install snapd -y
reboot
sudo snap install ngrok
ngrok version
```
